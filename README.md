# Calendar

### Clone the project:
``` $ git clone git@github.com:tawfiknasser/calendar.git ```
### Enter the project folder
``` $ cd calendar```
### Build with docker:
``` $ docker compose build```

### Run containers:

``` $ docker compose up```

### Open UI in browser: 
 ``` http://localhost:8080```