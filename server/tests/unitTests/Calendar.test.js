// const sinon = require('sinon'); to be used to inject models

describe('Calendar class unit test', () => {
  describe('getEvents', () => {
    it('', async () => { });
  });
  describe('updateEvent', () => {
    it('', async () => { });
  });
  describe('createEvent', () => {
    it('', async () => { });
  });
});
